import { MESSAGE_ROUTES } from './message.route'

import { MessageScreen } from '@ui/screen/message'

export const MessageRoutes = {
    [MESSAGE_ROUTES.MESSAGE]: {
        screen: MessageScreen
    }
}