import { DIRECTS_ROUTES } from './directs.route'

import { DirectsScreen } from '@ui/screen/directs'

export const DirectsRoutes = {
    [DIRECTS_ROUTES.DIRECTS]: {
        screen: DirectsScreen
    }
}